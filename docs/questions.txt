
Ερωτησεις για Διπλωματικη:
			
	- Experimentation:
		- Are the model I have chosen ok ?
		- προτεινομενο Δεδομενα + Μοντελλα για τεσταρισμα (μονο ενα ή πολλα) ? Ποια ειναι τα standarts οτι δουλευει ?
			- ποσα features ειναι expected to run ?
			- expected model size / number of weights (ωστε να εχει νοημα το GM) ?
	
	- multiple classes case scenario ? Is it necessary to evaluate it ?
		- multiple classes are not working, because we have no sense of direction other than randomly encountering a better position
		- On high number of classes => Search space (of possible models weights) / dimensionality of weights explosion

	- model size, how large does the model have to be ? is 100k enough ? More and it slows down significantly
		- as the model size increases, the weight messages become very costly
		
	- should i test any more non-differentiable loss functions ?

	- Για συνταξη της Διπλωματικης:	==========================================================================================

		- Ειναι τα Tasks που εχω σκεφτει σωστα και ξεχναω κατι ?:
			- Comparison with Gradient Descent ?
			- GUI / .env as User Interface ?
			- Μετρησεις προς το Experimentation Setup (τι κυματομορφες) ?
			
		- εχω βρει αυτα τα papers να βαλω στην διπλωματικη

	- Implementation Detail ===================================================================================================

	- velocity like simulated annealing ? Make velocity reduce over time so that swarm converges ... 
	- Χρησιμοποιω στον κωδικα καποια "κολπα" για να δουλευει το Kafka Streams με τους brokers πειραζει ?